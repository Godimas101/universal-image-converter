# SE Universal Audio Converter — Python dependencies
#
# Required for Audio Converter (WAV/XWM output):
#   ffmpeg.exe        — place next to the exe or add to PATH
#   xWMAEncode.exe    — place next to the exe or add to PATH (XWM output only)
#
# Required for Audio Editor:
numpy>=1.21.0
pygame>=2.1.0
#
# PyInstaller — for building the standalone exe:
#   pip install pyinstaller
